# Frontend (React 18 + Vite + TypeScript)

## Conventions
- TypeScript strict mode
- Frontend filenames: PascalCase for components, kebab-case for everything else
- No default exports in TypeScript

## React Components
- Always use shadcn components if applicable
- Make sure to check if an existing shadcn reusable component is created before adding new ones
